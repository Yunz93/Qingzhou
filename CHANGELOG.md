# Changelog

## Unreleased

- 模型卡片滑条改成当前模型的思考强度，不再用来换模型。
- 去掉底部手动点选强度横栏；闪电按钮始终可开关 Fast。
- 模型列表可设默认模型，写入 Pi `settings.json` 的 `defaultProvider` / `defaultModel`。
- 工作模式顶栏的主题切换和设置不再落在窗口拖拽区里，可以点。
- 更新技能时不再把 `.qingzhou-new` 临时目录列成重复技能；已留下的会在刷新时清掉。
- 详情栏「插件」改为打开 Pi 插件中心弹窗，一键安装官方目录里的包，不再预置推荐列表。
- 桌面窗口开启 Electron sandbox；webview 在附着时强制关闭 Node/preload，仅允许 http(s)。
- TaskService 终端逻辑抽到 TermController。
- TaskService 工作项逻辑抽到 WorkItemController。
- 高危命令检测按管道/链式分段，并识别引号拆词与简单十六进制混淆。
- Release 恢复 macOS x64（与 arm64 同机交叉编译），发版前跑 lint / typecheck / unit / integration；去掉未发布的 Linux AppImage 目标。
- 环回会话 cookie 改为 12 小时滑动过期。
- 自动审批的高危命令检测覆盖更多提权/管道执行/强制推送等模式，并做轻量空白规范化。
- Task/WorkItem 持久化改为合并写盘（默认跳过常规 fsync），关机时再强刷。
- Pi boot 的 get_state / get_messages / models / thinking / commands 并行拉取；资源扫描与 stats 不再挡住 pi_ready。
- 热切换复用已启动的 Pi 进程，槽位满时 LRU 回收空闲 warm 进程；默认 `QINGZHOU_MAX_PROCESSES` 提到 5。
- 流式 message.delta 走 O(1) 更新与廉价 eventId；stdin 写入尊重背压。
- 暖激活/snapshot 可省略 transcript 与工作板；前端对 delta 批次合批渲染并跳过完整 Zod。
- 流式期间拉长 sessionStorage 落盘间隔，减少主线程抖动。

## 0.1.19

- 点模型名打开「选择模型」列表，分「默认」和「推荐模型集」，当前模型打勾。
- 胶囊改成短标签，例如 `GPT-6 Astra 高`。
- 模型卡片去掉上下两处「选择强度」，底部改成分段横栏。
- 横栏档位按模型能力变化：没有推理只留关闭；`xhigh` / `max` 只有模型声明了才出现。

## 0.1.18

- 模型选择改成滑条卡片：可滑动切换、点下一个模型、选思考强度。
- 发送和停止合成同一个按钮；回复中且输入为空时显示停止。
- 切换模型后，对话里会出现「模型已从 A 更改为 B」提示。
- Git 详情可以预览尚未跟踪的新文件。
- 已经选了工作区后，随便聊聊和选家目录文件夹不再报不在允许范围。

## 0.1.17

- 运行中与 Pi 断连后会自动重连并重新同步会话，不再只能靠刷新页面恢复。
- 模型输出期间不再打断输入；排队中的追加和修正消息可以直接编辑。
- 设置页会识别 Pi 从环境变量、`models.json` 等来源加载的认证，不再把未配置认证的模型混入可用列表。
- 上下文用量窗口恢复不透明背景，避免和对话内容叠在一起。
- 应用图标更新为更轻快的小舟形象。
- 新对话在连接建立后会重新读取本机 Pi 会话，首次打开不再偶尔缺少可恢复会话。
- 精简环境下安装 Pi 会补齐 Unix 系统路径，不再因找不到 `sh` 或基础命令而假成功。
- 工作台项目名放大放在顶栏正中，用按钮菜单切换；「新建任务」挪到搜索框右侧。
- 对话模式可以「随便聊聊」，不必先选文件夹。
- 插件栏已安装和推荐分开；settings 里已有的包不再显示成未安装，失败回滚也不会误删原来的包。
- 额度不足等模型错误不会在再次发送时被清掉；任务抽屉也会显示。
- 技能栏恢复「检查更新」，去掉导出 HTML。
- 桌面版详情栏浏览器用 webview 打开互联网网页，不再受网站禁止嵌入限制。
- 约定在资源还没读到时显示「正在读取」，切换会话不再短暂露出上一份内容。
- 终端输出不再把整个工作台缓存反复写入，文件树只在打开文件/Git 时加载。
- 发送失败时文字和图片留在输入框，错误会显示在界面上；其它成功请求不会把这条错误清掉。
- 对话顶栏在会话列表未固定时避开 macOS 红绿灯，不再和「会话」按钮叠在一起。
- 任务对话不能再用普通发送绕过执行记录：结束后要先重新打开，空闲时从工作台补充要求。
- 新建项目/任务/对话、补充要求和 Git 提交都等请求成功再关窗口，失败时输入还在。
- 详情栏终端跟侧栏同一层底，暗色不再单独铺一块灰，内外不再有色差。
- 输入框可切换「追加」和「排队」：回车按当前模式发送，Shift+Enter 走另一种。
- 引擎支持时，模型和思考菜单可打开 Fast 模式。
- 文件预览长行会换行显示。

## 0.1.16

- 详情栏检查技能更新失败时显示中文原因，不再出现 `Invalid WebSocket payload`。
- 详情栏技能检查/更新和插件安装进度在切换 tab 后仍保留。
- macOS 后台 Pi 进程改走 Electron Helper，不再在程序坞出现多个 exec 图标。
- 应用内检查更新改为和墨知一样读 `latest.json`，不再打 GitHub API，避免「限制了检查次数」。
- 打版本标签时 Release 会附带 `latest.json`（含各平台安装包地址和 SHA-256）。
- 技能检查遇到 GitHub 限流时也会改走公开压缩包对比，不再只显示「限制了检查次数」。

## 0.1.15

- 详情栏「技能」可检查并更新用户目录里来自 Git / GitHub 的系统技能。
- 系统技能检查更新不再依赖 GitHub API 权限：SSH 远程改走 HTTPS，API 被拒时用公开压缩包对比内容。
- 工作台字体和间距收紧：对话正文、侧栏会话、详情技能列表和开关更接近系统控件比例。
- 安装推荐插件时按 Pi 要求一次只装一个包，避免 `Unexpected argument`。

## 0.1.14

- macOS 安装脚本能对上 `dist/` 前缀的 SHA256SUMS，且不再因中文逗号在 bash 3.2 下报 `name unbound variable`。
- 安装推荐插件时改用轻舟自己的 npm 缓存，避免 `~/.npm` 属于 root 时把 Pi 打崩；失败时只显示简短中文说明。
- 推荐插件只有真正装上后才显示「已安装」；下载失败会撤回写入的 Pi 设置，可以再点安装。

## 0.1.13

- 应用图标改为黑底、居中的白色水墨小船剪影。

## 0.1.12

- 发送失败会把文字和图片写回输入框；切会话不再丢掉后台流式内容；停止后等引擎结束后再发下一条。
- 时间线显示用户图片，并支持复制、克隆会话和回到最新。
- 工作会话切到对话后仍留在侧栏；看板不再重复显示项目名。
- 检查更新遇到 GitHub 403 时打开发布页，不再卡死。
- 详情栏「插件」可一键安装推荐扩展：pi-web-access、pi-memory、rpiv-todo、pi-subagents、pi-mcp-adapter、context-mode。
- 对话输入框改成更接近 Cursor / Codex 的原生卡片：大圆角、底栏工具、圆形发送键。

## 0.1.11

- 应用图标改为圆角山水乌篷船。
- 工作台顶栏项目选择框按当前项目名收窄，不再拉满一行。

## 0.1.10

- 应用内更新与墨知对齐：检查时必须有带校验的平台安装包，进程内下载并显示进度，校验通过后再替换，失败不会退回网站安装脚本。
- 设置页可自动检查、忽略某个版本，并查看发布说明。

## 0.1.9

- 产品名改为轻舟 / Qingzhou。旧的 `MOWEN_*` 环境变量和 `~/.mowen` 数据目录仍可用。
- 应用图标改为暗底折纸小船，不再用 π。
- Release 不再等待 GitHub `macos-13` Intel 排队，先发 Apple 芯片和 Windows。
- API 请求失败时在界面展示服务商返回的原因，不再变成空白或 `[object Object]`。
- OpenAI 订阅登录换 token 会走 `HTTPS_PROXY` / 系统代理，避免浏览器过了、轻舟进程不过。

## 0.1.8

- 终端 tab 改为 xterm.js，输入、滚动、选中复制和粘贴按原生终端来。
- 桌面版启动时检查更新，有新版本时在顶栏一键更新并重启。
- 去掉界面里多余的说明文字。
- 对话、审批、看板和检查器的交互收紧（发送/停止合一、工具折叠、审批倒计时等）。
- 应用重启后，未完成的工作目标不再卡在「进行中」，会标成已暂停并允许继续。
- HTTP 接口拒绝非本机 Host 和外来 Origin，降低 DNS rebinding 风险。
- 桌面更新会校验 Release 里的 `SHA256SUMS.txt`。
- Release 同时打包 macOS arm64 与 Intel x64。
- 修复桌面版重复注册 `qingzhou:pick-folder` 导致启动失败。
- 修复桌面版启动时再拉起一个带独立图标的 Electron 进程。
